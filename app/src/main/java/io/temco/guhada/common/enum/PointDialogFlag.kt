package io.temco.guhada.common.enum

/**
 * Point Dialog type enum
 * @author Hyeyeon Park
 * @since 2019.08.16
 */
enum class PointDialogFlag(val flag: Int) {
    CONFIRM_PURCHASE(0), WRITE_REVIEW(1), SAVE_SIZE(2)
}